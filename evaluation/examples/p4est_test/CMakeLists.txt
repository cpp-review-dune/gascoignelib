set(P4EST_EXEC P4est_test)

FILE(GLOB_RECURSE HEAT_SOURCE ${CMAKE_CURRENT_SOURCE_DIR}/*.cc)
ADD_EXECUTABLE(${P4EST_EXEC} ${HEAT_SOURCE})

target_compile_definitions(${P4EST_EXEC} PUBLIC
	${GASCOIGNE_DEFINITIONS}
)

target_include_directories(${P4EST_EXEC} PUBLIC
	${GASCOIGNE_INCLUDE_DIRS}
	${CMAKE_CURRENT_SOURCE_DIR}
)

target_link_libraries(${P4EST_EXEC} 
	GascoigneStd
)

set(ALL_CXX_SOURCE_FILES ${ALL_CXX_SOURCE_FILES} ${HEAT_SOURCE} PARENT_SCOPE)

