SET(NAVIERSTOKES2D_SRC
  main1.cc 
)

SET(_SRC ${GASCOIGNESTD_SOURCE_DIR}/Examples/NavierStokes2D)
IF(COPY_PARAMFILES)
  ADD_CUSTOM_TARGET(COPY ALL cp ${_SRC}/*.param ${_SRC}/*.inp .)
ENDIF(COPY_PARAMFILES)

ADD_EXECUTABLE(NavierStokes2D ${NAVIERSTOKES2D_SRC})


